%h1 Are you sure you want to delete the domain #{@domain.name}?

= form_for(@domain, :html => { :method => :delete }) do |f|
  = submit_tag t('destroy'), :class => 'btn btn-danger'
  = link_to :cancel, @domain


