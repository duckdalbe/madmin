- if obj.errors.any?
  .alert.alert-error
    %strong #{pluralize(obj.errors.count, "error")} prohibited this form from being saved:
    %ul
      - obj.errors.full_messages.each do |msg|
        %li.text-error= msg
