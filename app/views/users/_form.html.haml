= form_for([@domain, @user], :html => { :class => 'form-horizontal' }) do |f|
  = render :partial => 'shared/form_errors', :locals => { :obj => @user }
  -# TODO: let admins uplevel others to admin — but don't let them uplevel anyone to superadmin.
  .control-group
    = f.label :name, :class => 'control-label'
    .controls
      .input-append
        - if @user.created_at.blank?
          #{f.text_field(:name)}
        - else
          #{f.text_field(:name, :disabled => true)}
        %span.add-on @#{@domain.name}
  .control-group
    = f.label :password, :class => 'control-label'
    .controls= f.password_field :password
  .control-group
    = f.label :password_confirmation, :class => 'control-label'
    .controls= f.password_field :password_confirmation
  .control-group
    = f.label :forward_destination, :class => 'control-label'
    .controls= f.text_field :forward_destination
  .control-group
    = f.label :role, :class => 'control-label'
    .controls= f.collection_select :role, User::ROLES, :to_s, :humanize if (current_user.superadmin? && ! @user.postmaster?)
  .actions
    = f.submit
